# maestro/flows/fullscreen-video-playback.yaml
#
# E14: 全屏模式视频播放 + 切换验证
# 切换到全屏模式 → 进入视频浏览 → 验证视频播放 → 切换下一个视频 → 验证视频切换成功
#
appId: com.cleanpic.android
---
- launchApp
# 关闭可能出现的「发现新版本」弹窗（测试设备版本可能落后于线上已发布版，不影响被测功能）
- tapOn:
    id: "update_later_button"
    optional: true
- extendedWaitUntil:
    visible:
      id: "settings_button"
    timeout: 10000
- waitForAnimationToEnd

# 切换到全屏模式
- tapOn:
    id: "settings_button"
- waitForAnimationToEnd
- tapOn: "全屏上下"
- waitForAnimationToEnd
- tapOn:
    id: "back_button"
- waitForAnimationToEnd

# 切到「视频」Tab（首页默认照片 Tab，start_video 仅在视频 Tab 下出现）
- tapOn:
    text: "视频"
    optional: true
- waitForAnimationToEnd
# 进入视频浏览
- tapOn:
    id: "start_video"
- waitForAnimationToEnd
- tapOn:
    text: "授权"
    optional: true
- waitForAnimationToEnd
- tapOn:
    text: "Allow"
    optional: true
- waitForAnimationToEnd
- tapOn:
    id: "start_video"
    optional: true
- waitForAnimationToEnd

# 等待第一个视频加载和播放
- assertVisible:
    id: "exit_button"

# 截图：第一个视频播放中
- takeScreenshot: maestro/screenshots/fullscreen-video-playing

# 验证顶部栏计数显示 "1 / N"
- assertVisible:
    id: "exit_button"

# 等待 2 秒让视频播放一段
- swipe:
    start: "50%, 50%"
    end: "50%, 50%"
    duration: 2000

# 再次截图验证第一个视频在播放
- takeScreenshot: maestro/screenshots/fullscreen-video-playing-2

# === 切换到第二个视频（点击保留按钮） ===
- tapOn:
    id: "keep_button"
- waitForAnimationToEnd

# 等待第二个视频加载
- swipe:
    start: "50%, 50%"
    end: "50%, 50%"
    duration: 2000

# 截图：第二个视频应显示不同内容（验证视频切换成功）
- takeScreenshot: maestro/screenshots/fullscreen-video-switch-1

# === 再切换一个视频（点击删除按钮） ===
- tapOn:
    id: "delete_button"
- waitForAnimationToEnd

# 等待第三个视频加载
- swipe:
    start: "50%, 50%"
    end: "50%, 50%"
    duration: 2000

# 截图：第三个视频画面
- takeScreenshot: maestro/screenshots/fullscreen-video-switch-2

# 验证顶部栏和退出按钮仍可见（UI 正常）
- assertVisible:
    id: "exit_button"

# 恢复轮播模式
- tapOn:
    id: "exit_button"
- waitForAnimationToEnd
- tapOn:
    id: "settings_button"
- waitForAnimationToEnd
- tapOn: "轮播相册"
- waitForAnimationToEnd
